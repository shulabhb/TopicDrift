export interface MeetingObjectiveFormProps {
  initialValue?: string;
  onSubmit?: (objective: string) => void;
  disabled?: boolean;
}

/**
 * TODO: Collect or confirm the meeting objective after explicit user consent.
 */
export function MeetingObjectiveForm(_props: MeetingObjectiveFormProps) {
  return null;
}
