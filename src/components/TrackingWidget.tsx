export interface TrackingWidgetProps {
  isTracking: boolean;
  statusLabel: string;
}

/**
 * TODO: Provide compact in-meeting tracking controls after user activation.
 */
export function TrackingWidget(_props: TrackingWidgetProps) {
  return null;
}
