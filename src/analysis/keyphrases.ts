/**
 * Extracts weighted keyphrases from normalized text.
 *
 * TODO: Implement local keyphrase extraction without external APIs.
 */
export function extractKeyphrases(_text: string, _limit = 8): string[] {
  return [];
}
